import React from 'react';
import { Link } from 'react-router-dom';

const Header = () => (
  <header>
    <h1>To-Do-List</h1>
    <a href={"/"}>Sign out</a>
  </header>
);

export default Header;